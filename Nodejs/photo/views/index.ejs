<% include head %>
    <h1><%=settings.titles%></h1>
    <p>Express stock photo application.</p>
    <div id="photos">
      <% photos.forEach(function(photo) { %>
        <div class="photo">
          <h2><%=photo.name%></h2>
          <a href='###'>
            <img src='<%=photo.path%>'/>
          </a>
        </div>
      <% }) %>
    </div>
<% include footer.ejs %>